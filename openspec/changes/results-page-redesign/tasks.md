## 1. De-duplicate the "Your Salary" Display

- [ ] 1.1 In `app/components/Card/Result.vue`, add a `showUserSalary` prop (`Boolean`, default `true`) and wrap the existing `card-result--salaries` section (lines ~29-43) in `v-if="showUserSalary"`. Verify: `pnpm vitest run app/components/**/tests/Result.spec.ts` (new file, see 1.2) passes for both `true` and `false`.
- [ ] 1.2 Create `app/components/Card/tests/Result.spec.ts` (component currently has no test despite carrying real logic — `cardClasses`, `chipData` computeds) covering: default render shows the salary section; `showUserSalary="false"` hides it; `comparison` prop drives the correct chip variant (well-paid/underpaid/fairly-paid). Verify: test file passes and covers the new prop branch.
- [ ] 1.3 In `app/components/Section/Adzuna/Comparison.vue` and `app/components/Section/Government/Comparison.vue`, pass `:show-user-salary="false"` to `CardResult`. Neither component has an existing unit test today (confirmed — `app/components/Section/**` isn't in `vitest.config.ts`'s `coverage.include`, per `CODE_STANDARDS.md` §8, so this is not a mechanically-enforced gap). Verify: manual browser check on both `/salary` and `/benchmark` confirms the salary figure no longer renders inside either card, and `e2e/search.spec.ts` (confirm it doesn't assert on the removed text first) still passes.

## 2. KPI Summary Row

- [ ] 2.1 Decide final component name/location (design.md leaves this open — e.g. `app/components/Section/Results/KpiSummary.vue`) and scaffold it accepting: `userSalary`, `currencySymbol`, `marketAverage` (Adzuna), `diffPercent`, `isUnderpaid`, `hasJobsData`, `marketLow`, `marketAverageGov` (ONS mean/p50), `marketHigh`, `hasGovernmentData` as props — all sourced from existing `useLocationEngine` return values, no new composable logic. Verify: component renders with mock props in a new test file (see 2.3).
- [ ] 2.2 Implement the 4 cards per `specs/results-page-kpi-summary/spec.md`'s "KPI Summary Row" requirement: Your Salary (always shown when `userSalary` is set), Live Market Average + variance (only when `hasJobsData`), Government Benchmark + variance (only when `hasGovernmentData`), highlighted 75th-percentile ceiling card using `marketHigh` (only when `hasGovernmentData`, since `marketHigh` is sourced from ONS micro data). Verify: unit tests cover each of the spec's 4 scenarios (full data, no live data, no government data, no user salary).
- [ ] 2.3 Create `app/components/Section/Results/tests/KpiSummary.spec.ts` (path matches 2.1's final location) covering the 4 scenarios from 2.2. Verify: `pnpm vitest run` passes for the new file.
- [ ] 2.4 Add i18n keys for the KPI row's card labels (e.g. "Your Salary", "Live Market Average", "Government Benchmark", "75th Percentile Ceiling") to `i18n/locales/en-GB/{sections,card}.json` and mirrored `en-US` equivalents — reuse existing keys (`card.result.your-salary`, `card.result.market-average`) where the copy is identical rather than duplicating. Verify: `pnpm lint` (includes the i18n structure-lint check) passes.

## 3. MCA Regional Modifier Breakdown

- [ ] 3.1 In `app/components/Section/Score/Mca.vue`, add a 4th breakdown item rendering `verdict.modifier` when it is not equal to `1`, per `specs/results-page-kpi-summary/spec.md`'s "MCA Regional Modifier Breakdown" requirement. Per design.md decision 3, this is a new small labeled stat row (not a 4th `AmIChartRange`, since the modifier is a multiplier, not a 0-100 percentile) — e.g. showing the multiplier and a directional description (above/below national baseline). Verify: renders when `modifier !== 1`, omitted when `modifier === 1`.
- [ ] 3.2 Add i18n keys for the modifier breakdown label/description to `i18n/locales/en-GB/mca.json` and `en-US/mca.json` (mirror the existing `mca.breakdowns.{live,micro,macro}` key shape). Verify: `pnpm lint` passes.
- [ ] 3.3 `app/components/Section/Score/Mca.vue` has no existing unit test today (confirmed — same `coverage.include` gap as 1.3). Create `app/components/Section/Score/tests/Mca.spec.ts` covering: modifier bar renders with a non-1 modifier; modifier bar is omitted when modifier is exactly 1; existing gauge/confidence-bar/breakdown-toggle behavior is unaffected. Verify: test file passes.

## 4. Restyle Job Listing Cards

- [ ] 4.1 In `app/components/AmI/Card/Role.vue`, restyle the template to match the reviewed mockup's denser layout: add a company-initial avatar (derived from the existing `company` prop, no new prop needed), and give the salary range and the existing `salaryMaxComparison` chip more visual prominence (e.g. salary range in its own highlighted box, badge repositioned near the top). No changes to props, `salaryMaxComparison`, or `comparisonChipAttributes` computed logic — this is template/style only. Verify: existing behavior (pay-rise/pay-cut/no-change chip logic) is unchanged by inspection/tests.
- [ ] 4.2 Create `app/components/AmI/Card/tests/Role.spec.ts` (confirmed no test currently exists, despite `AmICardRole` being a base `AmI/**` component with real logic per `CODE_STANDARDS.md` §8) covering the existing `salaryMaxComparison` branches (positive, negative, zero, no `userSalary`) to lock in behavior before/through the restyle. Verify: test file passes.
- [ ] 4.3 Confirm `app/components/Section/Reed/JobListing.vue` and `app/components/Section/Jooble/JobListing.vue` (thin wrappers around `AmICardRole`) render correctly with the restyled card with no changes needed to either wrapper file. Verify: manual visual check via the dev server for a search where `dataProvider` resolves to `reed`/`jooble` (use `devProviderOverride`, gated by `process.dev`/`import.meta.dev` per AGENTS.md's Dev-Gate Rule — confirm the override still works pre-existing, don't introduce a new one).

## 5. Reorder Results Page Sections

- [ ] 5.1 In `app/pages/salary/[title]/[country]/[[location]].vue`, reorder the template to: KPI summary row (new, from section 2) → MCA score (`SectionScoreMca`) → Adzuna distribution (`SectionAdzunaComparison`) → Government benchmark (`SectionGovernmentComparison`) → Take Action / recruiter grid → job listings carousel. Preserve all existing `v-if`/`v-show` guards unchanged (`hasGovernmentData`, `hasJobsData`, `hasRecruiters`, `showUserSelection`, etc.) — only their position in the template changes. Verify: `pnpm typecheck` passes and a manual browser check confirms the new order renders correctly for a search with full data.
- [ ] 5.2 Apply the identical reorder to `app/pages/benchmark/[title]/[country]/[[location]].vue`, keeping both pages structurally identical per design.md's goal of not letting them diverge. Verify: manual browser check on `/benchmark/...` mirrors 5.1's result.
- [ ] 5.3 Verify the `ui-fixes` capability's updated requirements are met: MCA card and the highlighted 75th-percentile KPI card are the only full-semantic-color cards on the page; the KPI row renders as a multi-column grid on desktop widths. Manual check against `specs/ui-fixes/spec.md`'s two scenarios in this change.

## 6. Full Verification

- [ ] 6.1 Run `pnpm test:verify` (lint, unit/coverage, e2e, Firestore rules) and confirm it passes with no regressions before considering this change ready to validate/archive.
