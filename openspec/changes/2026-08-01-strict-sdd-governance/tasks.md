# Tasks: Strict SDD Governance

- [x] 1. Initialize `cspell` and configure `cspell.config.json` with project dictionary.
- [x] 2. Create `scripts/structure-lint.ts` to enforce naming and adjacent test requirements.
- [x] 3. Overhaul `eslint.config.mjs` with strict formatting and TypeScript rules.
- [x] 4. Update `tsconfig.json` with strict compiler flags.
- [x] 5. Combine checks into a unified `pnpm lint` command in `package.json`.
- [x] 6. Resolve blocking linter errors on legacy codebase by auto-fixing and downgrading rules to warnings.
- [x] 7. Adjust `structure-lint.ts` to allow Nuxt auto-namespaced single-word Vue components.
