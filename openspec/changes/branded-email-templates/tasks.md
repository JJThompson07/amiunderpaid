## 1. Core Branded Email Utility

- [ ] 1.1 Create `server/utils/emailTemplate.ts` implementing `renderBrandedEmail()` with responsive HTML table container, brand logo header (`/amiunderpaid-logo.png` / `/benchmarkmyrole-logo.png`), primary color accents (`#1cabb0` / `#dd8c40`), `escapeHtml()` sanitization, and helper renderers for data boxes, credential callouts, CTA buttons, and team sign-offs ("Best regards,\nThe Team at AmIUnderpaid" / "The Team at BenchmarkMyRole"). `renderBrandedEmail()` MUST take `brand` and `siteUrl` as explicit caller-supplied arguments — it MUST NOT read `useRuntimeConfig().public.siteUrl` internally (see design.md Decision 4: one Vercel project serves all three brand domains off a single deployment, so that value cannot represent "the current domain").
- [ ] 1.2 In the same file, implement `resolveBrandFromHost(event: H3Event): { brand: 'amiunderpaid' | 'benchmarkmyrole'; siteUrl: string }` using `getRequestHost(event)` / `getRequestProtocol(event)`, classifying `brand: 'benchmarkmyrole'` when the host contains `benchmarkmyrole`, else `'amiunderpaid'` (mirroring the hostname check in `app/plugins/tenant.ts`).
- [ ] 1.3 Create unit tests in `server/utils/tests/emailTemplate.spec.ts` covering default rendering, brand variations (AmIUnderpaid vs. BenchmarkMyRole), HTML escaping, CTA button markup, data box row formatting, credential box styling, optional field omissions, and `resolveBrandFromHost()` classification for both a `benchmarkmyrole` host and a non-`benchmarkmyrole` host (mock `getRequestHost`/`getRequestProtocol`). Verify with `pnpm vitest run server/utils/tests/emailTemplate.spec.ts`.

## 2. Lead Notification & Candidate Receipt Integration

- [ ] 2.1 Update `server/api/user/leads/submit.post.ts` to resolve `{ brand, siteUrl }` via `resolveBrandFromHost(event)` and format the recruiter lead notification email using `renderBrandedEmail()` with a structured candidate data box (Name, Email, Role, Location) and a "View Lead in Dashboard" CTA button. Pass the **raw** `name`/`searchedRole`/`location` values into the template (not the pre-escaped `safeName`/`safeSearchedRole`/`safeLocation` locals) since `renderBrandedEmail()`'s `escapeHtml()` is now the single point of HTML escaping for `message.html` — reusing the already-escaped `safe*` values would double-encode entities. The existing `safe*` values remain correct as-is for the Firestore `leads` document write and are unaffected.
- [ ] 2.2 Update `server/api/user/leads/submit.post.ts` to format the candidate confirmation email using `renderBrandedEmail()` with the same resolved `{ brand, siteUrl }`, a reassurance message, and a sign-off from "The Team at AmIUnderpaid" / "The Team at BenchmarkMyRole" per the resolved brand (raw `agencyName`, not `safeAgencyName`, per the escaping note in 2.1).
- [ ] 2.3 Update unit tests in `server/api/user/leads/tests/submit.spec.ts` to assert that both recruiter and candidate mail payloads contain the new branded HTML structure and matching plaintext summaries, for both a mocked `benchmarkmyrole` request host and a mocked `amiunderpaid` request host. Verify with `pnpm vitest run server/api/user/leads/tests/submit.spec.ts`.

## 3. Recruiter Access Request, Onboarding & Rejection Integration

- [ ] 3.1 Update `server/api/user/recruiter/request-access.post.ts` to resolve `{ brand, siteUrl }` via `resolveBrandFromHost(event)` and persist them on the created `users` document as `site` and `siteUrl` fields, so `accept.post.ts`/`reject.post.ts` can recover the applicant's brand later regardless of which domain the approving admin is on.
- [ ] 3.2 Update unit tests in `server/api/user/recruiter/tests/request-access.spec.ts` to assert the created document contains `site`/`siteUrl` matching a mocked `benchmarkmyrole` request host and a mocked `amiunderpaid` request host. Verify with `pnpm vitest run server/api/user/recruiter/tests/request-access.spec.ts`.
- [ ] 3.3 Update `server/api/admin/recruiters/accept.post.ts` to read `data.site` / `data.siteUrl` off the fetched recruiter document (falling back to `'amiunderpaid'` / `config.public.siteUrl` when absent, for documents created before task 3.1) and format the welcome/approval email using `renderBrandedEmail()` with that resolved brand, a highlighted temporary password credential box, password change advisory, and a "Log In to Your Dashboard" CTA button linking to `${siteUrl}/recruiter/login`.
- [ ] 3.4 Update `server/api/admin/recruiters/reject.post.ts` to read `data.site` off the fetched recruiter document (same fallback as 3.3) and format the application rejection email using `renderBrandedEmail()` with that resolved brand, a polite notification body, and team sign-off.
- [ ] 3.5 Update unit tests in `server/api/admin/recruiters/tests/accept.spec.ts` and `server/api/admin/recruiters/tests/reject.spec.ts` to assert that the queued mail documents are branded per a mocked `data.site: 'benchmarkmyrole'` document, per a mocked `data.site: 'amiunderpaid'` document, and per a legacy document with neither `site` nor `siteUrl` (fallback path). Verify with `pnpm vitest run server/api/admin/recruiters/tests/`.

## 4. Visual Verification via Example Email Dispatch

- [ ] 4.1 Prompt the user to provide their email address, then execute a test dispatch (e.g. via a one-off script or Firestore `mail` trigger / emulator / test send) sending one sample email of each of the 4 branded types (Recruiter Lead Notification, Candidate Confirmation Receipt, Recruiter Welcome / Approval with temporary password, and Recruiter Rejection) to that address — including at least one BenchmarkMyRole-branded sample — for live inbox visual verification. Confirm with the user that the emails render properly and link to the correct domain.

## 5. Verification & Gate Checks

- [ ] 5.1 Run full test and lint verification suite using `pnpm test:verify` (typecheck, lint, coverage 80%+ per-file gate, e2e tests, firestore rules) and ensure 100% pass rate.
