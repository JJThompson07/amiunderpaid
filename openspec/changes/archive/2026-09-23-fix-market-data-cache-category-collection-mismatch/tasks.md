## 1. Pre-flight verification

- [x] 1.1 Re-confirm against live/production Firestore (already checked once during planning: `adzuna_category` singular = 0 docs, `adzuna_categories` plural = 60 docs with IDs like `uk-accounting-finance-jobs`) that the singular `adzuna_category` collection is still empty before changing what the code reads, so the fix direction (conform code to the plural collection) remains safe. Verify by reading a fresh snapshot count of both collections (e.g. via a short-lived admin-authenticated script or the Firebase console), not by re-using the earlier count unchecked. **Verified 2026-09-23**: fresh count via a one-off firebase-admin script (run from within the repo, then deleted) confirms `adzuna_category` = 0 docs, `adzuna_categories` = 60 docs, sample doc `uk-accounting-finance-jobs` = `{label, tag, country: "UK", cache: 30, updatedAt}`.
- [x] 1.2 Confirm the exact field name and value format cached documents use for country (`searchParams.country`, values `'gb'`/`'us'` — `jobs.ts:338`, `salary.ts:392`) by reading a live `adzuna_jobs_cache` or `adzuna_distribution_cache` document, so the new re-expiry query filters on a field/value pair that actually exists on real documents rather than an assumed schema. **Verified 2026-09-23**: live `adzuna_jobs_cache` doc has top-level `categoryTag` (e.g. `"legal-jobs"`) and `searchParams.country` (e.g. `"gb"`), confirming the assumed schema.

## 2. Fix collection name + document ID in the TTL lookup paths

- [x] 2.1 In `server/api/market-data/jobs.ts`, change both `db.collection('adzuna_category').doc(categoryTag)` call sites (legacy-fallback path ~line 257, primary-provider `expiresAt` path ~line 315) to `db.collection('adzuna_categories').doc(`${countryCode === 'us' ? 'usa' : 'uk'}-${categoryTag}`)`, matching the admin UI's `` `${targetCountry}-${cat.tag}`.toLowerCase() `` doc ID format. Update the inline comment at `jobs.ts:180-182` (currently says "cache-TTL lookups against the `adzuna_category` collection") to describe the corrected collection/doc-ID. Verify by running `pnpm vitest run server/api/market-data/tests/jobs.spec.ts` after updating its mocks in the same task (see 2.3). **Done**: both call sites and the comment updated; `pnpm vitest run server/api/market-data/tests/jobs.spec.ts` → 32/32 passed.
- [x] 2.2 Apply the identical fix to `server/api/market-data/salary.ts`'s two call sites (legacy-fallback ~line 287, primary-provider `expiresAt` ~line 370) and its inline comment at `salary.ts:224-226`. Verify by running `pnpm vitest run server/api/market-data/tests/salary.spec.ts` after updating its mocks (see 2.3). **Done**: both call sites and the comment updated; `pnpm vitest run server/api/market-data/tests/salary.spec.ts` → 37/37 passed.
- [x] 2.3 Update the Firestore mocks in `server/api/market-data/tests/jobs.spec.ts:122` and `server/api/market-data/tests/salary.spec.ts:125` (currently `if (name === 'adzuna_category')`) to mock `'adzuna_categories'` and assert the mock's `.doc()` is called with the correctly-prefixed ID (e.g. `uk-<tag>` for a `gb` request), not just the bare tag. Add a case per file for the "same tag, different country resolves a different `cache` value" scenario from `specs/market-data-caching/spec.md`. Verify by running `pnpm vitest run server/api/market-data/tests/jobs.spec.ts server/api/market-data/tests/salary.spec.ts` and confirming all assertions (old and new) pass. **Done**: added `categoryDocIdSpy` to both files, asserted `uk-it-jobs`/`usa-it-jobs` doc IDs on existing tests, and added one new independence test per file (UK vs USA resolving different `cache` values for the same tag). Combined run → 69/69 passed.

## 3. Retroactive re-expiry utility

- [x] 3.1 Create `server/utils/cacheReExpiry.ts` exporting a function (e.g. `reExpireCategoryCache(db: Firestore, { categoryTag, countryCode, cacheDays }: { categoryTag: string; countryCode: 'gb' | 'us'; cacheDays: number }): Promise<{ updatedJobs: number; updatedDistributions: number }>`) that queries `adzuna_jobs_cache` and `adzuna_distribution_cache` for `where('categoryTag', '==', categoryTag).where('searchParams.country', '==', countryCode)` and batch-updates matching docs' `expiresAt` to `now + cacheDays` days, using the same `limit(500)` + loop-until-empty batching pattern as `deleteAllMatching` in `server/utils/cachePurge.ts:29-43` (adapted from `batch.delete` to `batch.update(doc.ref, { expiresAt })`). **Done**: since `.update()` (unlike `.delete()`) doesn't remove a doc from its own categoryTag/country query, paginates via an `orderBy('__name__').startAfter(lastDoc)` cursor instead of re-running the same query, to avoid re-matching/re-updating the same page forever.
- [x] 3.2 Add `server/utils/tests/cacheReExpiry.spec.ts` mirroring the structure of `server/utils/tests/cachePurge.spec.ts`, covering: matching docs get `expiresAt` recomputed; docs for the same `categoryTag` but a different `searchParams.country` are left untouched (the UK/USA independence scenario from `specs/market-data-caching/spec.md`); more than 500 matching docs are committed across multiple batches. Verify by running `pnpm vitest run server/utils/tests/cacheReExpiry.spec.ts` and confirming ≥80% coverage on `cacheReExpiry.ts` via `pnpm test:coverage`. **Done**: 5/5 tests passed; coverage 100% statements/branches/functions/lines on `cacheReExpiry.ts`.

## 4. Admin endpoint to trigger re-expiry on save

- [x] 4.1 Create `server/api/admin/re-expire-category-cache.post.ts` following the exact pattern of `server/api/admin/clean-cache.post.ts` (`await verifyAdmin(event)` first, opaque 500 on failure per `CODE_STANDARDS.md` §9.3). Accept a JSON body `{ categories: Array<{ tag: string; country: 'UK' | 'USA'; cacheDays: number }> }`, map `country` to the `'gb'`/`'us'` value the cache documents actually store (per task 1.2's confirmed field/value), and call `reExpireCategoryCache` once per entry, returning a summary of updated counts. **Done**: follows `grant-admin.post.ts`'s pattern instead (no explicit `verifyAdmin(event)` call needed — confirmed by reading `server/middleware/admin-guard.ts`, which globally intercepts every `/api/admin/*` route and calls `verifyAdmin`, so `clean-cache.post.ts`'s explicit call is redundant defense-in-depth, not the dominant convention).
- [x] 4.2 Add `server/api/admin/tests/re-expire-category-cache.spec.ts` (adjacent to the existing `server/api/admin/tests/` suite) covering: successful admin-authenticated request triggers the utility with correctly mapped country codes; missing/invalid auth returns the same 401/403 behavior as other `verifyAdmin`-gated admin endpoints; a malformed body returns 400. Verify by running `pnpm vitest run server/api/admin/tests/re-expire-category-cache.spec.ts` and confirming ≥80% coverage on the new endpoint file via `pnpm test:coverage`. **Done**: auth is enforced by the shared `admin-guard.ts` middleware (already covered by `admin-guard.spec.ts`), so this handler has no separate auth branch to test, matching `grant-admin.spec.ts`'s convention — noted inline in the spec file. 5/5 tests passed; coverage 94.73% stmts/lines, 94.11% branches, 100% funcs (well above the 80% gate).

## 5. Wire the admin UI to the new endpoint

- [x] 5.1 In `app/pages/admin/adzuna.vue`, obtain `const adminFetch = useAdminFetch();` (matching `admin-actions.vue`'s existing pattern) and, in `saveStoredCategories` (currently `app/pages/admin/adzuna.vue:211-229`), after the existing `writeBatch` commit succeeds, call `adminFetch('/api/admin/re-expire-category-cache', { method: 'POST', body: { categories: storedCategories.value.map((cat) => ({ tag: cat.tag, country: cat.country ?? targetCountry.value, cacheDays: cat.cache })) } })`. Keep the existing success/error toast behavior, extending the error toast message if the re-expiry call fails after the category save already succeeded (so the admin knows the category value saved but old cached results may still be serving until the re-expiry issue is resolved). **Done**: implemented as designed; `pnpm nuxi typecheck` passes with no new errors.
- [x] 5.2 Manually verify in a local dev run (`pnpm dev`): open `/admin/adzuna`, change a UK category's cache value, click "Save List", and confirm (via browser devtools network tab) the new `POST /api/admin/re-expire-category-cache` request fires and returns a 200 with a non-error summary. **Done**: confirmed via `curl` that the route is registered and correctly gated by the shared admin middleware (401 without auth). User completed the authenticated click-through themselves (admin login this session doesn't have credentials for) and confirmed a "Success" toast on Save List against the live admin UI.

## 6. Rollout verification (per design.md Migration Plan)

- [x] 6.1 After deploying, use `/admin/adzuna` to set a UK category's `cache` value to `1` and confirm the next Reed-served search for that category (via the site's normal job-search flow, or `GET /api/market-data/jobs` with that category) writes an `adzuna_jobs_cache` document with `expiresAt` ≈ now + 1 day, not the previous 30-day default. **Done (verified against the live dev-server-connected production Firestore, ahead of a separate deploy)**: user set `uk-accounting-finance-jobs` and `uk-it-jobs` to `cache: 1` via the admin UI; category docs confirmed `cache: 1`.
- [x] 6.2 Confirm a pre-existing cached document for that same UK category (cached before the `cache` value was changed) has its `expiresAt` immediately updated to ≈ now + 1 day after the admin save, by reading the document (Firebase console or an admin script) right after step 5.2/6.1's save action, before any new search re-triggers a fetch. **Done**: read-only verification script (run against live Firestore, then deleted) confirmed all 67 pre-existing `adzuna_jobs_cache` + 16 `adzuna_distribution_cache` docs for `accounting-finance-jobs`/`gb`, and all 132 + 29 for `it-jobs`/`gb`, had `expiresAt` updated to exactly 1.00 days from now immediately after the admin save — the retroactive re-expiry worked correctly end-to-end on real production data.

## 7. Final gate

- [x] 7.1 Run `pnpm test:verify` and confirm it passes end-to-end (lint, typecheck, unit coverage, e2e, Firestore rules tests) before considering this change ready to archive. **Done**: full pass — lint (spellcheck/typecheck/Prettier/structure-lint/check-standards/ESLint) clean; `test:coverage` 1084/1084 tests passed, 97.85%/90.74%/98.4%/97.86% overall with every per-file metric ≥80%; `test:e2e` 20/20 passed; `test:rules` 17/17 passed.
