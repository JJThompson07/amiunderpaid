## 1. De-duplicate the "Your Salary" Display

- [x] 1.1 REVISED during implementation: wrapping the entire `card-result--salaries` block in `v-if="showUserSalary"` would also hide the market-average figure, which is unique per-card information (Adzuna avg vs. Government avg) that must stay visible — only the user's own salary is the redundant part being deduplicated. In `app/components/Card/Result.vue`, added a `showUserSalary` prop (`Boolean`, default `true`); when `true`, renders the existing side-by-side salary/market-average pair unchanged; when `false`, renders a new `card-result--market-only` section showing just the market average, centered. Verify: `pnpm vitest run app/components/**/tests/Result.spec.ts` (new file, see 1.2) passes for both `true` and `false`.
- [x] 1.2 Create `app/components/Card/tests/Result.spec.ts` (component currently has no test despite carrying real logic — `cardClasses`, `chipData` computeds) covering: default render shows the salary section; `showUserSalary="false"` hides it; `comparison` prop drives the correct chip variant (well-paid/underpaid/fairly-paid). Verify: test file passes and covers the new prop branch.
- [x] 1.3 In `app/components/Section/Adzuna/Comparison.vue` and `app/components/Section/Government/Comparison.vue`, pass `:show-user-salary="false"` to `CardResult`. Neither component has an existing unit test today (confirmed — `app/components/Section/**` isn't in `vitest.config.ts`'s `coverage.include`, per `CODE_STANDARDS.md` §8, so this is not a mechanically-enforced gap). Verify: manual browser check on both `/salary` and `/benchmark` confirms the salary figure no longer renders inside either card, and `e2e/search.spec.ts` (confirm it doesn't assert on the removed text first) still passes.

## 2. KPI Summary Row

- [x] 2.1 Created `app/components/Section/Results/KpiSummary.vue`, accepting `userSalary`, `currencySymbol`, `hasJobsData`, `liveMarketAverage` (Adzuna `meanSalary`), `hasGovernmentData`, `govMarketAverage` (ONS mean/p50, `marketAverage`), `marketHigh`, `diffPercentGov` (`diffPercent`) as props — renamed from the original `marketAverage`/`marketAverageGov` sketch for clarity once both an Adzuna and a government average needed to coexist as distinct props. `isUnderpaid`/`isUnderpaidAdzuna` were not needed as separate props: variance colour is derived directly from the percentage (see 2.2) rather than a second boolean. All values sourced from existing `useLocationEngine`/`useJobs` return values, no new composable logic. Verify: component renders with mock props in `tests/KpiSummary.spec.ts` (2.3) — passes.
- [x] 2.2 Implemented the 4 cards per `specs/results-page-kpi-summary/spec.md`'s "KPI Summary Row" requirement: Your Salary (`v-if="userSalary"`), Live Market Average + variance (`v-if="hasJobsData"`, variance only when `userSalary` is set, computed in-component via `getRawDiffPercentage` since `useLocationEngine` doesn't expose a live-diff value), Government Benchmark + variance (`v-if="hasGovernmentData"`, using the passed-in `diffPercentGov`), highlighted 75th-percentile ceiling card using `marketHigh` with a full-colour `primary`→`secondary` gradient (`v-if="hasGovernmentData"`, matching `ui-fixes` spec's "MCA + ceiling card are the only full-colour cards" rule). Verify: unit tests cover each of the spec's 4 scenarios (full data, no live data, no government data, no user salary) — 5/5 passing.
- [x] 2.3 Created `app/components/Section/Results/tests/KpiSummary.spec.ts` covering the 4 scenarios from 2.2 plus a variance-colour check. Verify: `pnpm vitest run app/components/Section/Results/tests/KpiSummary.spec.ts` — 5/5 passing.
- [x] 2.4 Added `sections.json`'s `kpi.{live-market,ceiling-label,ceiling-description}` keys to `i18n/locales/en-GB/sections.json` and the mirrored `en-US/sections.json`. Reused `card.result.your-salary` (identical copy, "Your salary") and `sections.government.title` (identical copy, "Government Benchmarks") directly in the component rather than duplicating either — no new `card.json` keys needed. Verify: `pnpm exec eslint` clean on the new component/test file; `pnpm exec prettier --write` applied to both locale files (no diff, already valid JSON/formatting).

## 3. MCA Regional Modifier Breakdown

- [x] 3.1 In `app/components/Section/Score/Mca.vue`, added a 4th breakdown item (`v-if="verdict.modifier !== 1"`) rendering the multiplier (e.g. "1.25x") and a directional description ("{percent}% above/below the national baseline for this location") via two new computeds, `modifierMultiplierLabel`/`modifierDescription`. Per design.md decision 3, this is a plain labeled stat row (mirroring `AmIChartRange`'s label/value/description layout), not a 4th `AmIChartRange`, since the modifier is a multiplier, not a 0-100 percentile. Verify: renders when `modifier !== 1`, omitted when `modifier === 1` — covered in 3.3.
- [x] 3.2 Added `mca.breakdowns.modifier.{label,above,below}` keys to `i18n/locales/en-GB/mca.json` and `en-US/mca.json`, mirroring the existing `mca.breakdowns.{live,micro,macro}` shape. Verify: `pnpm exec eslint`/`pnpm exec prettier --write` clean, `pnpm typecheck` exit 0.
- [x] 3.3 Created `app/components/Section/Score/tests/Mca.spec.ts` (confirmed no test existed previously) covering: modifier row renders above/below-baseline copy for modifier > 1 and < 1; modifier row omitted when modifier === 1; score/label/confidence-bar rendering unaffected; breakdown toggle open/close; the 3 existing `AmIChartRange` rows still render. Verify: `pnpm vitest run app/components/Section/Score/tests/Mca.spec.ts` — 6/6 passing.

## 4. Restyle Job Listing Cards

- [x] 4.1 In `app/components/AmI/Card/Role.vue`, restyled the template: added a company-initial avatar circle (derived from the existing `company` prop via a new `companyInitial` computed — purely presentational, no new prop), moved the comparison chip into the header next to the avatar for prominence, and put the salary range in its own highlighted `bg-slate-50` box with larger type and the pay-rise/pay-cut/no-change caption underneath. No changes to props, `salaryMaxComparison`, or `comparisonChipAttributes` — template/style only. Verify: existing behavior locked in by `tests/Role.spec.ts` (4.2) — 6/6 passing.
- [x] 4.2 Created `app/components/AmI/Card/tests/Role.spec.ts` covering the `salaryMaxComparison` branches (positive/pay-rise, negative/pay-cut, zero/no-change, omitted when no `userSalary`) plus the new avatar initial and salary-range rendering. Verify: `pnpm vitest run app/components/AmI/Card/tests/Role.spec.ts` — 6/6 passing.
- [x] 4.3 Confirmed via the dev server (`http://ami-uk.localhost:3000/salary/software-developer/uk/scotland`) that the restyled card renders correctly for the default Adzuna path and, after setting the pre-existing `devProviderOverride=reed` cookie, for `SectionReedJobListing`'s wrapped path too — avatar, salary box, and chips all render as expected in both. The `devProviderOverride=jooble` cookie value didn't visibly switch the rendered provider in this environment (pre-existing dev-tooling behavior, unrelated to this change), but `SectionJoobleJobListing.vue` passes the same core props to `AmICardRole` as `SectionReedJobListing.vue` (confirmed by direct comparison — Jooble additionally passes `raw-salary`, which only affects the pre-existing `salaryRange` computed's raw-string branch, untouched by this restyle), so the Reed check exercises the same restyled template path. No changes needed to either wrapper file.

## 5. Reorder Results Page Sections

- [x] 5.1 In `app/pages/salary/[title]/[country]/[[location]].vue`, reordered the template to: KPI summary row (`SectionResultsKpiSummary`, new) → MCA score (`SectionScoreMca`) → Take Action / recruiter grid → Market Data (`SectionAdzunaComparison` + `SectionGovernmentComparison`, kept in their existing side-by-side flex container since design.md only calls for reordering blocks, not restructuring their internal layout) → job listings carousel → Action/Negotiation. REVISED during implementation (see design.md decision 5): recruiters were first placed directly before the carousel (last data section); user feedback after reviewing the live page asked for them directly under MCA instead, ahead of the Adzuna/Government cards, which is the order that shipped. All existing `v-if`/`v-show` guards (`hasGovernmentData`, `hasJobsData`, `hasRecruiters`, `showUserSelection`, etc.) preserved unchanged — only position moved. Verify: `pnpm typecheck` exit 0; manual browser check at `http://ami-uk.localhost:3000/salary/software-developer/uk?compare=45000` (a search matching the seeded `TanuKeyTech` recruiter) confirms the final order renders correctly: KPI row → MCA → Take Action (recruiter card) → Adzuna/Government cards → job carousel → Negotiation.
- [x] 5.2 Applied the identical reorder to `app/pages/benchmark/[title]/[country]/[[location]].vue`. Verify: manual browser check at `http://bmr.localhost:3000/benchmark/software-developer/uk/london?compare=45000` (the BenchmarkMyRole brand's local domain, per `nuxt.config.ts`'s domain list — `/benchmark` isn't served on the `ami-uk.localhost` domain) mirrors 5.1's result exactly, with the brand's own accent colour on the highlighted ceiling card.
- [x] 5.3 Verified the `ui-fixes` capability's requirements: the MCA card and the highlighted 75th-percentile KPI card are the only full-semantic-color cards visible (KPI row's other 3 cards, the recruiter card, and the Adzuna/Government cards use white/neutral backgrounds with small colour badges); the KPI row renders as a 4-column grid at desktop width (confirmed in both browser checks above).

## 6. Recruiter Card Refinement

- [x] 6.1 Restyled `app/components/AmI/RecruiterButton.vue` per design.md decision 6: replaced the full-card translucent brand-colour wash (`rounded-3xl`, tinted background) with a white `rounded-2xl` card (`shadow-sm`/`hover:shadow-md`) matching the KPI/`AmICardRole`/`AmICardAction` card convention; avatar changed from a `w-12 h-12 rounded-xl` square to a `w-10 h-10 rounded-full` circle (logo image, or a light tint of the recruiter's own `brandBgColour` behind the fallback briefcase icon when no logo is set); added `flex flex-col h-full` so cards in the same grid row align evenly regardless of title length. The recruiter-configured icon/logo, title/button copy (including `{location}`/`{agency}`/`{incentive}` wildcard replacement), and button background/text colour are all unchanged — only the card chrome changed. Verify: existing behavior locked in by a new `tests/RecruiterButton.spec.ts` (6.2) — 7/7 passing.
- [x] 6.2 Created `app/components/AmI/tests/RecruiterButton.spec.ts` (confirmed no test existed previously, despite `AmIRecruiterButton` being a base `AmI/**` component with real logic — `replaceWildcards`/`displayTitle`/`displayButtonText`) covering: wildcard replacement for `{location}`/`{agency}`/`{incentive}` (including the benchmark-vs-salary path incentive difference); fallback copy when `title`/`buttonText` aren't configured; fallback avatar/icon when no `logoUrl` is set; the configured logo rendering when present; the configured brand colours applying to the CTA button; the `click` event emitting the card. Verify: `pnpm vitest run app/components/AmI/tests/RecruiterButton.spec.ts` — 7/7 passing.
- [x] 6.3 Manually verified via the dev server at `http://ami-uk.localhost:3000/salary/software-developer/uk?compare=45000` that the restyled recruiter card renders correctly in its new position (directly under the MCA score, per task 5.1) — white background, circular logo, hover shadow, fitting visually with the KPI row and job cards above/below it.

## 7. Full Verification

- [x] 7.1 Ran `pnpm test:verify` — lint (spellcheck, typecheck, Prettier, structure-lint, check-standards, ESLint), unit/coverage (112 test files, 863 tests, all passing, no per-file coverage threshold failures), e2e (20/20 Playwright tests passing across chromium/firefox/ssr), and Firestore rules (13/13 passing) all passed with no regressions. Exit code 0.
