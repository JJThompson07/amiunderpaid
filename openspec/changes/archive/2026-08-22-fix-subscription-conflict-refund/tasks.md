## 1. Pre-flight verification (irreversible/infra-dependent — must run before any code change)

- [x] 1.1 In Stripe test mode, create a subscription-mode checkout session with a `basicCount > 0` recurring line item (£30/mo) plus a one-off exclusive-months line item (£100), matching `create-checkout.post.ts`'s shape, with a trial (`subscription_data.trial_end`), and complete it with a test card via Playwright automation against the real `checkout.stripe.com` page. **Observed:** `subscription.latest_invoice` is populated synchronously and already `status: 'paid'`; `amount_paid: 10000` (the full upfront one-off amount — the recurring item's own charge is deferred to trial end but the one-off item invoices immediately even under a trial).
- [x] 1.2 Repeated 1.1 for the `basicDiscount: 100` shape (recurring line item at £0/mo, **no** `subscription_data` at all — matches `create-checkout.post.ts`'s `basicCount > 0 && monthlyTotal > 0` guard being false) plus the same £100 one-off item. **Observed:** `subscription.status: 'active'` (no trial), `latest_invoice.amount_paid: 10000` — confirms the audit's "£0/mo subscription with a fully-paid upfront invoice" scenario is real.
- [x] 1.3 Repeated 1.1 for a basic-only cart (no exclusive months, so no one-off line item) with a trial. **Observed:** `subscription.status: 'trialing'`, `latest_invoice.status: 'paid'` (a $0 invoice is trivially payable) with `amount_paid: 0` — confirms the "nothing collected" case.
- [x] 1.4 **Critical finding, changes the implementation:** `Invoice.payment_intent` does not exist on this account's pinned Stripe API version (`2026-03-25.dahlia`) — `expand: ['subscription.latest_invoice.payment_intent']` resolves to `undefined` even on the fully-paid £100 invoices from 1.1/1.2, with no error. The real reference is the Invoice Payments API: `stripe.invoicePayments.list({ invoice: <id> }).data[0].payment.payment_intent` — confirmed to return one `invoice_payment` record with a real `pi_...` ID for the paid cases, and an **empty array** for the £0 trial case from 1.3. The `amount_paid > 0` guard itself is correct and unchanged; only the payment-intent lookup mechanism changes. **`design.md` updated accordingly — task 2.1 below reflects the corrected approach, not the audit's original code sample.**

## 2. Implement refund-then-cancel and fix territoryConflict reset

- [x] 2.1 In `server/api/stripe/webhook.post.ts`, replace the `else if (session.subscription)` branch in `queueRefundAndAlert` (`:63-64`) per `design.md`'s corrected code sample: `stripe.subscriptions.retrieve(session.subscription, { expand: ['latest_invoice'] })`, then when `invoice.amount_paid > 0`, look up the payment intent via `stripe.invoicePayments.list({ invoice: invoice.id })` (NOT `invoice.payment_intent`, which doesn't exist on this API version — see task 1.4) and refund it, then `stripe.subscriptions.cancel(...)`. Leave the `session.mode === 'payment'` branch and the final `else` throw at `:65-67` unchanged.
- [x] 2.2 In the same file, fix `territoryConflict` (`:122`, assigned at `:252`) leaking across a retried `db.runTransaction` attempt: have the transaction callback return a discriminated result (`{ conflict: false } | { conflict: true; error: Error }`) instead of writing to the outer closure variable, and branch on the resolved return value at `:291` instead of the closure variable.
- [x] 2.3 In `server/api/stripe/tests/webhook.spec.ts`, rewrite the test at `:278` ("handles a subscription conflict by cancelling the subscription") to mock `stripe.subscriptions.retrieve` returning a paid `latest_invoice` (`amount_paid > 0`) AND mock `stripe.invoicePayments.list` returning one record with `payment.payment_intent` set, and assert both `mockRefundsCreate` is called with that payment intent ID and `stripe.subscriptions.cancel` is called; rename the test to reflect refund + cancel.
- [x] 2.4 In the same file, add a new test for the zero-`amount_paid` (trialling) case: mock `stripe.subscriptions.retrieve` returning a `latest_invoice` with `amount_paid: 0`, and assert `stripe.invoicePayments.list` is NOT called, `mockRefundsCreate` is NOT called, `stripe.subscriptions.cancel` IS called, and no alert email is sent.
- [x] 2.5 Add a test for the `territoryConflict` retry fix: simulate `db.runTransaction` invoking the callback twice (first attempt hits the conflict branch and returns, second attempt succeeds with no conflict), and assert `queueRefundAndAlert` is NOT called after a successful retry.

## 3. Correct the spec

- [x] 3.1 Confirm `openspec/changes/fix-subscription-conflict-refund/specs/stripe-checkout-security/spec.md` (already drafted) matches the implemented behavior from step 2, adjusting scenario wording if the pre-flight findings in step 1 changed the guard condition.

## 4. Verification

- [x] 4.1 Run local verification: `pnpm vitest run`. 360/360 passing.
- [x] 4.2 Run `pnpm lint`. Clean (spellcheck, typecheck, prettier, eslint, structure-lint, check-standards).
