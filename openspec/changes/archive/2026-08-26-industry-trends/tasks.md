## 1. Database & Sync Endpoint

- [x] 1.1 In `server/api/admin/`, create a new endpoint `sync-trends.ts`. Ensure it is protected by the standard admin checks. Implemented as `server/api/admin/sync-trends.post.ts` — verified live that every existing admin endpoint (`grant-admin.post.ts`, `clean-cache.post.ts`) uses a `.post.ts` suffix, unlike `server/api/market-data/`, so this follows that real convention rather than the bare filename in the task text. Calls `verifyAdmin(event)` explicitly (matching `clean-cache.post.ts`), on top of the `admin-guard.ts` middleware that already covers all `/api/admin/**` routes.
- [x] 1.2 In `sync-trends.ts`, derive the list of active categories by querying distinct `categoryTag` values from the `adzuna_jobs_cache` collection (each cached search document already stores its `categoryTag` — see `server/api/market-data/jobs.ts`). Do NOT read `adzuna_category`: that collection is a sparse, manually-populated cache-duration override table (`{ cache: <days> }`) that nothing in the codebase writes to, so it cannot be treated as a registry of tracked categories. Implemented as the pure, unit-tested `extractActiveCategoryCountryPairs()` in `server/utils/adzunaHistory.ts`, fed by a `.select('categoryTag', 'searchParams')` projection query so only the two needed fields are read.
- [x] 1.3 For each category and country (UK/USA), query the Adzuna History API (`/v1/api/jobs/${country}/history`) with `category` and `months` query params (`months=1` for ongoing monthly syncs, `months=12` for the initial backfill) — confirmed live against the real Adzuna API: `months=12` returns exactly 12 monthly data points. Run these calls with bounded concurrency (e.g. a small batch/`Promise.all` chunk size, not one giant unbounded `Promise.all` and not a fully sequential loop) — with N categories × 2 countries this is a real number of sequential network round-trips, and Vercel serverless functions have an execution time limit, so a naive fully-sequential loop risks timing out the whole sync before it completes. Implemented with `CONCURRENCY = 4` chunked batches.
- [x] 1.4 Format the returned Adzuna history data into a clean time-series array (e.g., `[{ month: '2023-01', average: 54000 }, ...]`), **sorted chronologically by month**. Adzuna's response does not guarantee key order — confirmed live: a real `months=12` call returned its 12 month keys in randomized order, not chronological — so this step must explicitly sort, not just trust the response shape. Implemented as the pure, unit-tested `formatHistoryMonths()` in `server/utils/adzunaHistory.ts`.
- [x] 1.5 Write the formatted data into a new Firestore collection called `adzuna_industry_trends`, using a composite document ID like `${country}_${categoryTag}` **and** an explicit `country` field (e.g. `'gb'`/`'us'`) stored on the document itself — the composite ID alone cannot be efficiently queried by country (Firestore document IDs aren't prefix-queryable without a range-query workaround), so task 2.2's "all docs for a country" read needs a real indexed field, not string-parsing of the ID. Use `set(..., { merge: true })` or array unions so that monthly updates smoothly append new data points to the existing document without overwriting history. Backfill (`months=12`) replaces the stored `history` array wholesale; monthly delta (`months=1`) merges the new point into existing history inside a transaction, de-duped by month. **Addendum**: also stores a human-readable `label` field (e.g. `"IT Jobs"` for `it-jobs`), resolved once per country via Adzuna's `/categories` endpoint (not per-category — one extra call per country total) — verified live that this endpoint returns `{ results: [{ label, tag }] }`. Without this, the UI would have nothing but raw slugs to show on toggle pills and the chart legend.
- [x] 1.6 Wrap each category/country sync call so one failure (e.g. Adzuna 429s or errors on category 6 of 15) does not abort the whole batch — catch per-call errors, continue the remaining categories, and return/log a summary of which categories succeeded vs. failed so a partial sync is visible rather than silently incomplete. Each `syncOne()` call catches its own error and returns `{ status: 'ok' | 'error', error? }`; the endpoint returns a `results` array plus `synced`/`failed` counts.

## 2. Public API

- [x] 2.1 Create a new public endpoint `server/api/market-data/industry-trends.ts` (no `.get.ts` suffix — matches this folder's existing convention: `jobs.ts` and `salary.ts` are bare filenames for GET endpoints; the `.get.ts`/`.post.ts` suffix pattern is only used in `server/api/admin/`).
- [x] 2.2 Read the requested country from the query params (e.g., `country=gb`), normalize/validate it the same way `market-data/jobs.ts` does (map `usa`/`us` → `us`, everything else → `gb` — don't pass the raw query value straight into a Firestore `where`), then query `adzuna_industry_trends` with `.where('country', '==', countryCode)` using the field added in task 1.5. Implemented via the pure, unit-tested `normalizeCountryCode()` in `server/utils/adzunaHistory.ts`.
- [x] 2.3 Structure the response so the frontend easily receives a list of categories mapped to their historical data points. Response is `{ country, industries: [{ categoryTag, label, history }] }` — includes the human-readable `label` resolved during sync (task 1.5's addendum) rather than just the raw slug, since the UI needs a real name for each toggle pill / legend entry.
- [x] 2.4 Wrap the Firestore read in `defineCachedFunction` (same pattern as `fetchFromProviders` in `market-data/jobs.ts`), keyed by country, with a cache duration appropriate for data that only changes once a month (e.g. `maxAge: 60 * 60 * 24`) — this is a public, unauthenticated endpoint and every other market-data endpoint in this repo caches its Firestore/external reads; this one shouldn't be the exception.

## 3. Frontend UI (Industry Trends Page)

- [x] 3.1 Create `app/pages/insights/industry-trends.vue`.
- [x] 3.2 Add page metadata (`definePageMeta` or `useHead`) to ensure strong SEO titles/descriptions (e.g., "Industry Salary Trends"). Implemented via `useSeoMeta` + `$siteBrand` conditional, matching `how-it-works.vue`'s exact pattern, reading from a new `meta.json` "industry-trends" entry (en-GB and en-US, each with a `benchmark` sub-object) — this repo's SEO metadata lives in `meta.json`, not the per-page locale file.
- [x] 3.3 On mount, fetch the data from `/api/market-data/industry-trends`.
- [x] 3.4 Import and initialize `echarts`. Create a large, responsive chart container (e.g., `h-[500px] w-full`) rendered inside a premium white bento card with soft shadows. Lifecycle (init on mount, dispose + resize-listener cleanup on unmount) mirrors the existing `Territory/Map.vue` echarts usage.
- [x] 3.5 Map the fetched data to ECharts `series` format (one line per industry). Colors are resolved live via `getComputedStyle` against this repo's actual CSS custom properties (`--color-primary-500`, `--color-secondary-500`, etc. — same technique as `Territory/Map.vue`), so the palette automatically matches whichever brand ($siteBrand) is active instead of hardcoding one brand's colors.
- [x] 3.6 Build the control panel below the chart — pill toggles, `selectedIndustries` reactive array, Select All/Clear All, watcher re-renders the chart on selection change.
- [x] 3.7 Add a time-range selector (Last 6 / Last 12 / All Time) that filters the X-axis data points dynamically.
- [x] 3.8 No hardcoded strings (`CODE_STANDARDS.md` §6): created `i18n/locales/en-GB/insights.json` and `i18n/locales/en-US/insights.json`. **Also registered both in `i18n/locales/{en-GB,en-US}/index.ts`** — the task text didn't mention this step, but every locale JSON file in this repo must be explicitly imported and added to that file's returned object, or `$t('insights....')` silently resolves to nothing; verified by reading `index.ts` before assuming the new files would just work.

## 4. Navigation & Sitemap

- [x] 4.1 In the main site navbar (`app/components/AmI/NavBar.vue`), add an "Insights" navigation item. Create a dropdown or group that links to `/insights/industry-trends`. Add the "Insights" label to `navbar.json` (en-GB and en-US) and reference it via `$t()`, matching how the rest of `NavBar.vue`'s existing items are localized. Added as a new group in the "ORDINARY USER GROUPS" (`navLinks`) array, between "Resources" and "Partners" — this page is public-facing, not admin/recruiter-only.
- [x] 4.2 In `server/routes/sitemap.xml.ts`, explicitly add `'/insights/industry-trends'` to the `staticRoutes` array to ensure Google indexes it.

## 5. Verification

- [x] 5.1 Run `pnpm typecheck` to ensure no typing regressions. Passes clean (exit 0), no errors beyond the known/ignorable `vue-router/volar/sfc-route-blocks` framework warning.
- [x] 5.2 Hit the admin endpoint via a REST client to backfill 12 months of data, verifying Firestore populates correctly, and verify partial-failure handling from 1.6 by testing a case where one category's Adzuna call fails (e.g. an invalid category tag) — confirm the rest of the batch still completes. No admin session was available in this environment to call the endpoint over HTTP, so the underlying sync logic was verified directly against real services instead (same credentials the server uses): `extractActiveCategoryCountryPairs` found 50 real category/country pairs from production `adzuna_jobs_cache`; a real `months=12` Adzuna call for `gb/it-jobs` returned 12 chronologically-sorted months, written to and read back from `adzuna_industry_trends` correctly; a `.where('country', '==', 'gb')` query found it. Seeded 9 real categories (6 gb + 3 us) end-to-end. Partial-failure isolation verified separately: a batch of `['it-jobs', 'this-category-does-not-exist-xyz', 'sales-jobs']` against the real Adzuna API returned 2 ok / 1 error (`HTTP 400`), proving one bad category doesn't abort the rest.
- [x] 5.3 Verify the chart renders smoothly with all lines, and toggles operate without lag. Verified in-browser against the real seeded data: 6-line chart renders with correct labels/colors/smooth curves; "Clear All" empties the chart; toggling a single pill isolates that one line with correct selected-state styling; the time-range selector (Last 6 Months) correctly narrows the X-axis and redraws all lines.
- [x] 5.4 Write unit tests for the new server-side logic (`CODE_STANDARDS.md` §8 — required for all new `server/**` utilities, and this repo enforces 80% coverage per file): at minimum, the category-derivation logic (1.2), the month-formatting/sorting function (1.4), and the country-normalization logic (2.2). Place them in `tests/` adjacent to the files being tested, per the existing convention. Run `pnpm test:coverage` and confirm the new/modified files clear the 80% threshold before considering this change complete. Added `server/utils/tests/adzunaHistory.spec.ts` (14 new test cases). `adzunaHistory.ts` achieves 100% statements/branches/functions/lines coverage — verified via the generated coverage HTML report, since v8's text reporter hides fully-covered files from its table by default (which is also why pre-existing 100%-covered files like `adzuna.ts` don't appear there either — not a sign of a problem).

## 6. Post-Archive Follow-Up (see proposal.md and design.md decisions 8-11)

- [x] 6.1 Extract sync logic into `server/utils/industryTrendsSync.ts`, shared by both `sync-trends.post.ts` and the new cron route. Pace Adzuna calls in batches of 20/minute (`chunkForRateLimit()` — pure, unit-tested, added to `server/utils/adzunaHistory.ts`) and retry once with a 10s backoff on HTTP 429. Verified live against Adzuna's documented limit (25 req/min, 250/day, 1000/week, 2500/month — provided directly, not guessed): the same 50-category sync that failed 9/50 unpaced completed 50/50 with this pacing.
- [x] 6.2 Add `server/api/cron/sync-trends.get.ts` (outside `/api/admin/` — `admin-guard.ts`'s `verifyAdmin` would otherwise reject Vercel's cron request, which carries no Firebase session), authenticated via `CRON_SECRET` matching Vercel's own reserved-name convention for auto-sent cron auth (confirmed against Vercel's docs: cron always sends GET, not POST; the secret arrives as `Authorization: Bearer <value>`). Runs `runIndustryTrendsSync(1)` — monthly delta only, never a 12-month backfill. Add `vercel.json` scheduling it `0 3 1 * *` (1st of the month). Registered `cronSecret` in `nuxt.config.ts` runtimeConfig and documented `CRON_SECRET` in `.env.example`. Verified live: unauthenticated and wrong-secret requests both return 401; a real authenticated run completed 50/50.
- [x] 6.3 Replace the 6-8 color palette with 30 colors (`app/assets/css/main.css`, `--chart-1`..`--chart-30`), fixed hue-rotation with moderate saturation/lightness, reordered with a fixed stride (13, coprime with 30) so adjacent legend entries land ~156° apart in hue rather than sweeping smoothly through the wheel. **Correction**: an initial attempt declared these inside the Tailwind `@theme` block — confirmed live via the compiled stylesheet that Tailwind v4 tree-shakes `@theme` tokens unless an actual utility class references them, so only `chart-1` survived out of 30. Moved to a plain `:root` block instead (outside `@theme`), confirmed fixed by re-inspecting the compiled CSS. Also fixed colors being unstable across toggles (previously assigned by index within the _visible_ filtered subset, not the full list).
- [x] 6.4 Add `generateColorScale(hex)` to new `shared/utils/color.ts` (converts hex→HSL, re-derives lightness per stop from a fixed 50-900 ramp, hue/saturation preserved) so each industry's legend pill can use the same light-tint-background/dark-text badge treatment used elsewhere in this app (e.g. MCA bracket badges) instead of a flat pill or a neutral pill with a color-swatch dot. Chart line uses the `500` stop; pill uses `100`/`800`. Unit tested in `shared/utils/tests/color.spec.ts` (13 cases), including an exact-value check cross-referenced against an independent HSL-to-RGB reference implementation, not just testing the module against itself. Documented the pattern in `CODE_STANDARDS.md` §5 for future dynamic-color needs.
- [x] 6.5 Fix tooltip overflow: `confine: true` plus a `max-height`/`overflow-y: auto` via `extraCssText`, since an axis-trigger tooltip with 20-30 series selected could grow taller than the viewport. Verified in-browser: tooltip now stays within the chart card and scrolls instead of overflowing the page.
- [x] 6.6 Re-run full verification after all of the above: `pnpm typecheck` and `pnpm lint` clean; `pnpm test` — 393/393 unit tests pass (13 new for `color.ts`, 4 new for `chunkForRateLimit`); manual full-catalogue backfill via the real cron route (50/50 succeeded); visual verification of the new palette, pill styling, and tooltip fix in-browser.
